{erl_first_files, [gen_indira_listener]}.
{erl_opts, [debug_info]}.
{port_specs, [
  {"priv/af_unix_drv.so", ["c_src/af_unix.c"]}
]}.
{edoc_opts, [
  {overview, "src/overview.edoc"},
  {source_path, ["src", "examples"]},
  todo
]}.
{post_hooks, [
  % append "@private" EDoc marker to indira_proto_lexer (leex doesn't make it
  % easy)
  {compile, "sed -i -e '1i%%% @private' -e '/@private/d' src/indira_proto_lexer.erl"}
]}.
